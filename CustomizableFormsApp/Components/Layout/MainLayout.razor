@inherits LayoutComponentBase

<div class="page">
    <div class="sidebar">
        <NavMenu />
    </div>

    <main class="main">
        <div class="top-row px-4">
            <a href="https://learn.microsoft.com/aspnet/core/" target="_blank">About</a>
        </div>

        <article class="content px-4">
            @Body
        </article>
    </main>
</div>

@* These are comments, they should NOT be visible in the browser.
   If they are, it means the Razor rendering for this layout is also broken. *@
@* // Added for MouseEventArgs // Added for Any() *@